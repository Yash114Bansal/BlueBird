# Zero Token (Required)
ZERO_TOKEN=your_zero_token_here

# Database Configuration (Optional - will be fetched from Zero)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=evently
DB_USER=evently
DB_PASSWORD=evently123

# Redis Configuration (Optional - will be fetched from Zero)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_USE_TLS=false

# JWT Configuration (Optional - will be fetched from Zero)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRY_MINUTES=30
REFRESH_TOKEN_EXPIRY_DAYS=7

# SMTP Configuration (Optional - will be fetched from Zero)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_USE_TLS=true

# CORS Configuration (Optional - will be fetched from Zero)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting (Optional - will be fetched from Zero)
RATE_LIMIT_LOGIN=5
RATE_LIMIT_REGISTER=3
RATE_LIMIT_WINDOW=15